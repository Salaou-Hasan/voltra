// spawn_npc — create a non-player entity.
// args: [npc_id, x, y, kind]
const [npc_id, x, y, kind] = args;
const npc = {
  npc_id, x: x || 0, y: y || 0, kind: kind || "goblin",
  hp: 50, max_hp: 50, aggressive: true,
};
__neondb_set("npcs", npc_id, npc);
result = { ok: true, npc };
